import 'package:flutter/material.dart';

// Colors/styles used througout the app, change here to change everywhere
// TODO: Decide on colors to use
const Color tagColor = Colors.blue;
const Color textColor = Colors.white;
const Color backgroundColor = Color.fromARGB(255, 47, 47, 47);
const Color borderColor = Color.fromARGB(255, 212, 212, 212);

// Font Styles
const headingText = TextStyle(fontSize: 20, color: textColor);
const headingAccentText = TextStyle(fontSize: 20, color: tagColor);
// Body Text Styles
const bodyText = TextStyle(fontSize: 16, color: textColor);
const bodyAccentText = TextStyle(fontSize: 16, color: tagColor);
